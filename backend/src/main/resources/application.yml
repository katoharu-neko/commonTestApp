# backend/src/main/resources/application.yml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:8889/common_test_app?useSSL=false&serverTimezone=Asia/Tokyo
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    try-it-out-enabled: true
    persistAuthorization: true
    display-request-duration: true
    operations-sorter: method
    doc-expansion: none

# ← ここが今回のクラッシュ原因（必須）
jwt:
  # 32バイト以上の秘密鍵(Base64)。下は開発用サンプルです
  secret: "QkR0dGJrZ0p5c0o3cU1kV1Z2c1ZqV2ZrQ0pYTlR2M0l5cU1MN29kT1hYZ3ZHS2JXbGZFM1lxc1BOWk1SVGc9PQ=="
  expiration-ms: 2592000000  # 30日

# 開発時メール送信（SendGrid未設定ならログ出力のみ）
sendgrid:
  apiKey: ""  # 空なら送らずログ出力

app:
  mail:
    from: "no-reply@example.com"
  frontendBaseUrl: "http://localhost:3000"
  backendBaseUrl: "http://localhost:8080" 